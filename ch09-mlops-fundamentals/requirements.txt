# Chapter 9: MLOps fundamentals lab. Pinned, CI-tested versions.
# The [s3] extra on dvc is only needed when the DVC remote is MinIO or S3; the
# local-directory remote in version-data.sh runs with plain dvc. The MLflow lab
# does not use DVC at all.
mlflow==3.14.0
# mlflow pulls skops<1 for sklearn model serialization. skops 0.15.0
# (Sep 2026) rejects sklearn tree types as untrusted on save, so pin the
# version this lab was tested with.
skops==0.14.0
scikit-learn==1.9.0
numpy==2.4.6
dvc[s3]==3.67.1
