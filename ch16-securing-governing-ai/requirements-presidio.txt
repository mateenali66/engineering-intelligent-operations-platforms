# Optional production PII path for Chapter 16 (optional local run, not a CI job, Python 3.12).
# Presidio runs fully local on CPU with no API key and no torch. Its small-model
# NER is probabilistic (it mislabeled an SSN as ORGANIZATION in a measured run),
# which is why the deterministic regex scrub in detectors.py is the CI gate and
# Presidio is the documented production upgrade.
presidio-analyzer==2.2.362     # Requires-Python >=3.10,<3.14
presidio-anonymizer==2.2.362
# Install the spaCy model from the WHEEL URL, not `spacy download` (the CLI has an
# undeclared click dependency that breaks in a clean venv):
#   pip install https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.8.0/en_core_web_sm-3.8.0-py3-none-any.whl
